---
import { TypographyP } from '../ui/typography';
import { ZoomableImage } from '../ui/ZoomableImage';
---

<section class="section bg-neutral-950 py-12 md:py-24">
  <div class="container-custom">
    <div class="grid lg:grid-cols-2 gap-8 md:gap-12 items-center">
      <!-- Left: code editor image -->
      <div>
        <ZoomableImage
          client:load
          src="/assets/product/code-editor.svg"
          alt="Code Editor"
          className="w-full h-auto rounded-xl shadow-lg"
        />
      </div>

      <!-- Right: paragraph -->
      <div>
        <TypographyP className="text-gray-300" data-i18n="product.section2.description">
          Students open a challenge, write their solution directly in the browser,
          and run it with one click. The editor gives instant feedback on errors and
          outputs, turning trial and error into a smooth learning experience. No
          setup, no distractions — just code, learn, and improve.
        </TypographyP>
      </div>
    </div>
  </div>
</section>